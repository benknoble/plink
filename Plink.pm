#! /usr/bin/env perl

package Plink;

use v5.10;
use strict;
use warnings;
use autodie;

use open ':locale';
# implicit with
# use v5.10;
use feature 'unicode_strings';

main() unless caller(0);

sub main {
}

# return true
1;
